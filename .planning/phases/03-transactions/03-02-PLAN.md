---
phase: 03-transactions
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/actions/checkout.ts
  - app/api/webhooks/stripe/route.ts
  - app/(public)/checkout/success/page.tsx
  - app/(public)/listings/[id]/page.tsx
  - lib/email/order-confirmation.tsx
  - lib/email/admin-order-alert.tsx
  - tests/transactions/checkout.test.ts
  - tests/transactions/webhook-stripe.test.ts
  - tests/transactions/emails.test.ts
autonomous: true
requirements:
  - PURCH-01
  - PURCH-02
  - PURCH-03
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "Consumer can click Buy Now and be redirected to Stripe Checkout"
    - "Stripe webhook creates order record and marks listing sold atomically"
    - "Webhook is idempotent — duplicate events do not create duplicate orders"
    - "Consumer sees order confirmation page after successful payment"
    - "Buyer receives order confirmation email after payment"
    - "Admin receives order alert email when a sale occurs"
  artifacts:
    - path: "app/actions/checkout.ts"
      provides: "Server Action that creates Stripe Checkout Session"
      exports: ["createCheckoutSession"]
    - path: "app/api/webhooks/stripe/route.ts"
      provides: "Stripe webhook handler for checkout.session.completed"
      exports: ["POST"]
    - path: "app/(public)/checkout/success/page.tsx"
      provides: "Order confirmation page"
      min_lines: 40
    - path: "lib/email/order-confirmation.tsx"
      provides: "React Email template for buyer confirmation (NOTF-01)"
      exports: ["OrderConfirmationEmail"]
    - path: "lib/email/admin-order-alert.tsx"
      provides: "React Email template for admin alert (NOTF-02)"
      exports: ["AdminOrderAlertEmail"]
  key_links:
    - from: "app/(public)/listings/[id]/page.tsx"
      to: "app/actions/checkout.ts"
      via: "form action calling createCheckoutSession"
      pattern: "createCheckoutSession"
    - from: "app/actions/checkout.ts"
      to: "lib/stripe.ts"
      via: "stripe.checkout.sessions.create"
      pattern: "stripe\\.checkout\\.sessions\\.create"
    - from: "app/api/webhooks/stripe/route.ts"
      to: "lib/supabase/admin.ts"
      via: "createAdminClient for service_role mutations"
      pattern: "createAdminClient"
    - from: "app/api/webhooks/stripe/route.ts"
      to: "lib/resend.ts"
      via: "resend.emails.send for NOTF-01 and NOTF-02"
      pattern: "resend\\.emails\\.send"
    - from: "app/(public)/checkout/success/page.tsx"
      to: "orders table"
      via: "supabase query by stripe_checkout_session"
      pattern: "stripe_checkout_session"
---

<objective>
Build the complete Stripe purchase flow: "Buy Now" button redirects to Stripe Checkout, webhook creates the order and marks the listing sold, confirmation page shows order details, and two emails fire (buyer confirmation + admin alert).

Purpose: This is the core transaction spine. Every subsequent plan (documents, purchase history) depends on orders existing in the database.
Output: Working checkout-to-confirmation flow with webhook fulfillment and 2 email templates
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transactions/03-CONTEXT.md
@.planning/phases/03-transactions/03-RESEARCH.md
@.planning/phases/03-transactions/03-01-SUMMARY.md

@lib/stripe.ts
@lib/resend.ts
@lib/supabase/admin.ts
@lib/supabase/server.ts
@lib/validations/order.ts
@app/(public)/listings/[id]/page.tsx
@middleware.ts

<interfaces>
<!-- From lib/stripe.ts (created in Plan 01): -->
export const stripe: Stripe  // Stripe client singleton

<!-- From lib/resend.ts (created in Plan 01): -->
export const resend: Resend
export const ADMIN_EMAIL: string
export const FROM_EMAIL: string

<!-- From lib/supabase/admin.ts (existing): -->
export function createAdminClient(): SupabaseClient  // service_role client

<!-- From lib/validations/order.ts (created in Plan 01): -->
export const checkoutSchema: z.ZodObject<{ listingId: z.ZodString }>
export const orderStatusEnum: z.ZodEnum<[...]>
export type OrderStatus = z.infer<typeof orderStatusEnum>

<!-- From DB schema (002_schema.sql): -->
orders: { id, listing_id, buyer_id, seller_id, status, price_cents, stripe_payment_intent, stripe_checkout_session, created_at, updated_at }
order_documents: { id, order_id, document_type, storage_key, signed_at, signer_id, esign_ref, status, created_at, updated_at }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Checkout Server Action, Stripe webhook handler, and email templates</name>
  <files>app/actions/checkout.ts, app/api/webhooks/stripe/route.ts, lib/email/order-confirmation.tsx, lib/email/admin-order-alert.tsx, tests/transactions/checkout.test.ts, tests/transactions/webhook-stripe.test.ts, tests/transactions/emails.test.ts</files>
  <behavior>
    - createCheckoutSession with active listing returns Stripe redirect (mock stripe.checkout.sessions.create)
    - createCheckoutSession with inactive listing throws error
    - createCheckoutSession with no authenticated user redirects to /login
    - Webhook POST with valid signature + checkout.session.completed creates order, marks listing sold, inserts 2 order_documents rows
    - Webhook POST with valid signature for already-processed session is idempotent (no duplicate order)
    - Webhook POST with invalid signature returns 400
    - OrderConfirmationEmail renders to HTML string without errors
    - AdminOrderAlertEmail renders to HTML string without errors
  </behavior>
  <action>
1. **Implement test cases** in `tests/transactions/checkout.test.ts`:
   - Replace `it.todo()` stubs with real tests
   - Mock `@/lib/supabase/server` (createClient) and `@/lib/stripe` (stripe)
   - Test `createCheckoutSession(listingId)`:
     - Active listing: mocked Supabase returns listing, mocked Stripe returns session with url — assert `redirect` called with session.url
     - Inactive listing: mocked Supabase returns null — assert throws 'Listing not available'
     - No user: mocked auth.getUser returns null — assert redirect to /login

2. **Implement test cases** in `tests/transactions/webhook-stripe.test.ts`:
   - Mock `stripe.webhooks.constructEvent`, `createAdminClient`, `resend.emails.send`
   - Test POST handler:
     - Valid event: assert Supabase `.update` called on listings (status: sold), `.insert` called on orders, `.insert` called on order_documents with 2 rows
     - Duplicate: mock existing order returned from `.select` — assert no insert called
     - Invalid sig: mock constructEvent to throw — assert 400 response

3. **Implement test cases** in `tests/transactions/emails.test.ts`:
   - Use `render` from `@react-email/components` to render each template to string
   - Assert result contains expected content (order number, vehicle title)
   - Replace `it.todo()` for OrderConfirmationEmail and AdminOrderAlertEmail (leave NOTF-03/04 todos for Plan 03)

4. **Create `app/actions/checkout.ts`** — Server Action:
   - `'use server'` directive
   - `export async function createCheckoutSession(listingId: string)` — validate with checkoutSchema
   - Get user via `createClient().auth.getUser()` — redirect to /login if not authenticated
   - Fetch listing from DB: `.select('id, title, price_cents, status, seller_id').eq('id', listingId).eq('status', 'active').single()`
   - CRITICAL: price from DB, never from client
   - Create `stripe.checkout.sessions.create({ mode: 'payment', line_items, metadata: { listing_id, buyer_id, seller_id }, success_url, cancel_url })`
   - `success_url`: `${process.env.NEXT_PUBLIC_URL}/checkout/success?session_id={CHECKOUT_SESSION_ID}`
   - `cancel_url`: `${process.env.NEXT_PUBLIC_URL}/listings/${listingId}`
   - `redirect(session.url!)`

5. **Create `app/api/webhooks/stripe/route.ts`** — Route Handler:
   - `export async function POST(request: Request)`
   - Read raw body with `await request.text()` (NOT request.json() — critical for HMAC)
   - Get `stripe-signature` from headers
   - `stripe.webhooks.constructEvent(body, sig, process.env.STRIPE_WEBHOOK_SECRET!)` in try/catch — return 400 on failure
   - Handle `checkout.session.completed`:
     - Extract `listing_id`, `buyer_id`, `seller_id` from `session.metadata`
     - Idempotency: check if order exists with this `stripe_checkout_session` — return 200 if so
     - Use `createAdminClient()` (service_role) for all mutations
     - Update listing status to 'sold': `.update({ status: 'sold' }).eq('id', listing_id).eq('status', 'active')` — the `.eq('status', 'active')` prevents double-sale race
     - Insert order with status 'paid', price_cents from session.amount_total
     - Insert 2 order_documents rows (purchase_agreement + title_transfer, both status 'pending')
     - Send NOTF-01 (buyer confirmation) and NOTF-02 (admin alert) via resend.emails.send
     - Use `after()` from `next/server` to trigger async document pipeline (will be implemented in Plan 03 — for now just a placeholder comment `// TODO Plan 03: void generateAndSendDocuments(order.id)`)
   - Return `new Response('OK', { status: 200 })` for all events

6. **Create `lib/email/order-confirmation.tsx`** (NOTF-01):
   - React Email template with Coast branding (dark bg: #09090b, text: #fafafa)
   - Props: `orderNumber, vehicleTitle, priceCents, buyerName`
   - Content: "Order Confirmed" heading, vehicle name, price, "Documents are being prepared. You'll receive an email when they're ready to sign."
   - Include a CTA button linking to `/account/orders/{orderId}` with text "View Order"

7. **Create `lib/email/admin-order-alert.tsx`** (NOTF-02):
   - React Email template with Coast branding
   - Props: `orderNumber, vehicleTitle, priceCents, buyerName, buyerEmail`
   - Content: "New Order Alert" heading, vehicle details, buyer info, price
  </action>
  <verify>
    <automated>npm run test:unit -- tests/transactions/checkout.test.ts tests/transactions/webhook-stripe.test.ts tests/transactions/emails.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Checkout action creates Stripe session and redirects. Webhook creates order + marks listing sold + sends 2 emails. All 3 test files pass (non-todo tests green). Email templates render without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Order confirmation page and Buy Now button wiring</name>
  <files>app/(public)/checkout/success/page.tsx, app/(public)/listings/[id]/page.tsx</files>
  <action>
1. **Create `app/(public)/checkout/success/page.tsx`** — Order confirmation page:
   - Server Component — receives `searchParams.session_id`
   - Fetch order from DB using supabase (server client): `.from('orders').select('*, listings(title, year, make, model, vin, price_cents)').eq('stripe_checkout_session', session_id).single()`
   - If no order found (payment processing or invalid session), show a loading/pending state with message "Your order is being processed..."
   - Display: Coast-branded dark layout matching existing pages
     - Success checkmark icon
     - "Order Confirmed" heading
     - Order number (first 8 chars of order id, uppercased)
     - Vehicle: year make model
     - Amount paid: formatted from price_cents
     - Next steps section: "Documents are being prepared. You'll receive an email when they're ready to sign."
     - Link to "View Your Orders" → `/account/orders`
     - Link to "Continue Browsing" → `/listings`

2. **Update `app/(public)/listings/[id]/page.tsx`** — Wire Buy Now button:
   - Replace the disabled `<button>` stub with a working form
   - Import `createCheckoutSession` from `@/app/actions/checkout`
   - For authenticated users viewing an active listing: wrap button in a `<form action={...}>` that calls `createCheckoutSession.bind(null, listing.id)`
   - Use a client component `BuyNowButton` for pending state (useFormStatus) — show "Processing..." while Stripe session is being created
   - Keep "Log in to purchase" link for unauthenticated users
   - If listing status is 'sold', show "Sold" badge instead of buy button
  </action>
  <verify>
    <automated>npx tsc --noEmit app/\(public\)/checkout/success/page.tsx app/\(public\)/listings/\[id\]/page.tsx 2>&1 | head -10</automated>
  </verify>
  <done>Buy Now button on listing page submits to checkout action. Confirmation page renders order details with next-steps messaging. Both pages compile without errors. Listing page shows "Sold" badge for sold listings.</done>
</task>

</tasks>

<verification>
- `npm run test:unit -- tests/transactions/checkout.test.ts tests/transactions/webhook-stripe.test.ts tests/transactions/emails.test.ts` all pass
- `npx tsc --noEmit` passes for all new/modified files
- Buy Now button on listing detail page is wired to createCheckoutSession action
- Confirmation page fetches and displays order by session_id
- Webhook handler is idempotent and uses raw body for signature verification
</verification>

<success_criteria>
- Consumer can click "Buy Now" on a listing and be redirected to Stripe Checkout
- Stripe webhook creates order, marks listing sold, sends NOTF-01 and NOTF-02
- Consumer sees order confirmation page with order number, vehicle, amount, and next steps
- Webhook is idempotent against Stripe retries
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-transactions/03-02-SUMMARY.md`
</output>
