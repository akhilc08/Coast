---
phase: 03-transactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/stripe.ts
  - lib/dropbox-sign.ts
  - lib/resend.ts
  - lib/validations/order.ts
  - tests/transactions/checkout.test.ts
  - tests/transactions/webhook-stripe.test.ts
  - tests/transactions/pdf.test.ts
  - tests/transactions/webhook-dropbox.test.ts
  - tests/transactions/emails.test.ts
  - tests/transactions/checkout.spec.ts
  - tests/transactions/orders.spec.ts
autonomous: true
requirements:
  - PURCH-01
  - PURCH-02
  - DOC-01
  - DOC-02
  - DOC-05
  - NOTF-01
  - NOTF-02
  - NOTF-03
  - NOTF-04

must_haves:
  truths:
    - "Stripe, Dropbox Sign, and Resend client singletons are importable"
    - "Order Zod schemas validate checkout and order data shapes"
    - "All Wave 0 test stubs exist and run without error"
  artifacts:
    - path: "lib/stripe.ts"
      provides: "Stripe client singleton"
      exports: ["stripe"]
    - path: "lib/dropbox-sign.ts"
      provides: "Dropbox Sign API client"
      exports: ["signatureApi"]
    - path: "lib/resend.ts"
      provides: "Resend client singleton"
      exports: ["resend"]
    - path: "lib/validations/order.ts"
      provides: "Zod schemas for order/checkout data"
      exports: ["checkoutSchema", "orderSchema"]
  key_links:
    - from: "lib/stripe.ts"
      to: "process.env.STRIPE_SECRET_KEY"
      via: "env var"
      pattern: "STRIPE_SECRET_KEY"
    - from: "lib/resend.ts"
      to: "process.env.RESEND_API_KEY"
      via: "env var"
      pattern: "RESEND_API_KEY"

user_setup:
  - service: stripe
    why: "Payment processing for vehicle purchases"
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> Signing secret (create endpoint first)"
      - name: NEXT_PUBLIC_URL
        source: "Your app URL (e.g., http://localhost:3000 for dev)"
  - service: dropbox-sign
    why: "E-signing for purchase agreement and bill of sale"
    env_vars:
      - name: DROPBOX_SIGN_API_KEY
        source: "Dropbox Sign Dashboard -> API -> API Key"
  - service: resend
    why: "Transactional email delivery"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: ADMIN_EMAIL
        source: "Admin email address for order alert notifications"
---

<objective>
Install Phase 3 dependencies, create service client singletons (Stripe, Dropbox Sign, Resend), define order Zod schemas, and scaffold all Wave 0 test stubs for the transactions test suite.

Purpose: Every subsequent plan imports these clients and validates against these schemas. Wave 0 tests establish the verification contract for all Phase 3 work.
Output: 3 client singletons, order validation schemas, 7 test files (5 unit stubs + 2 e2e stubs)
</objective>

<execution_context>
@/Users/sickle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sickle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transactions/03-CONTEXT.md
@.planning/phases/03-transactions/03-RESEARCH.md
@.planning/phases/03-transactions/03-VALIDATION.md

@lib/supabase/admin.ts
@lib/supabase/server.ts
@lib/validations/listing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages, create service client singletons and order schemas</name>
  <files>lib/stripe.ts, lib/dropbox-sign.ts, lib/resend.ts, lib/validations/order.ts</files>
  <action>
1. Install packages:
   ```bash
   npm install stripe @dropbox/sign resend @react-email/components pdfkit
   npm install --save-dev @types/pdfkit
   ```

2. Create `lib/stripe.ts` — Stripe client singleton:
   - `import Stripe from 'stripe'`
   - Export `stripe` instance with `process.env.STRIPE_SECRET_KEY!`, pin `apiVersion` to `'2024-11-20.acacia'`, set `typescript: true`
   - Follow the pattern in `lib/supabase/admin.ts` (guard env var, server-only comment)

3. Create `lib/dropbox-sign.ts` — Dropbox Sign API client:
   - Import `SignatureRequestApi` from `@dropbox/sign`
   - Create and export `signatureApi` instance, set `signatureApi.username = process.env.DROPBOX_SIGN_API_KEY!`
   - Add server-only comment

4. Create `lib/resend.ts` — Resend client singleton:
   - `import { Resend } from 'resend'`
   - Export `resend` instance with `process.env.RESEND_API_KEY!`
   - Export `ADMIN_EMAIL = process.env.ADMIN_EMAIL!` for NOTF-02
   - Export `FROM_EMAIL = 'Coast <no-reply@coastautos.com>'` as default sender

5. Create `lib/validations/order.ts` — Zod schemas:
   - `checkoutSchema`: `z.object({ listingId: z.string().uuid() })` — input for checkout Server Action
   - `orderSchema`: full order shape matching the `orders` table columns (id, listing_id, buyer_id, seller_id, status, price_cents, stripe_payment_intent, stripe_checkout_session, created_at, updated_at)
   - `orderStatusEnum`: `z.enum(['pending_payment', 'paid', 'documents_sent', 'documents_signed', 'complete', 'cancelled', 'refunded'])`
   - `documentStatusEnum`: `z.enum(['pending', 'sent', 'signed', 'voided'])`
   - `documentTypeEnum`: `z.enum(['purchase_agreement', 'title_transfer'])`
   - Export all schemas and inferred types
  </action>
  <verify>
    <automated>npx tsc --noEmit lib/stripe.ts lib/dropbox-sign.ts lib/resend.ts lib/validations/order.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 4 files compile, export correct types, and env var guards are in place</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for all Phase 3 requirements</name>
  <files>tests/transactions/checkout.test.ts, tests/transactions/webhook-stripe.test.ts, tests/transactions/pdf.test.ts, tests/transactions/webhook-dropbox.test.ts, tests/transactions/emails.test.ts, tests/transactions/checkout.spec.ts, tests/transactions/orders.spec.ts</files>
  <action>
Create `tests/transactions/` directory with 7 test files. Follow existing project pattern: unit tests use `*.test.ts`, e2e tests use `*.spec.ts`, e2e stubs use `test.fixme()`.

1. `tests/transactions/checkout.test.ts` (PURCH-01):
   - `describe('createCheckoutSession')` with stubs:
     - `it('creates Stripe checkout session for active listing and redirects')`
     - `it('throws error if listing is not active')`
     - `it('throws error if user is not authenticated')`
   - All tests should be `it.todo(...)` for now — implementation in Plan 02

2. `tests/transactions/webhook-stripe.test.ts` (PURCH-02):
   - `describe('Stripe webhook handler')` with stubs:
     - `it('creates order and marks listing sold on checkout.session.completed')`
     - `it('is idempotent — skips if order already exists for session')`
     - `it('returns 400 on invalid signature')`
     - `it('returns 200 for unhandled event types')`
   - All `it.todo(...)`

3. `tests/transactions/pdf.test.ts` (DOC-01, DOC-02):
   - `describe('generatePurchaseAgreement')` with stubs:
     - `it('returns a non-empty Buffer')`
     - `it('generated PDF contains vehicle VIN')`
   - `describe('generateBillOfSale')` with stubs:
     - `it('returns a non-empty Buffer')`
     - `it('generated PDF contains buyer and seller names')`
   - All `it.todo(...)`

4. `tests/transactions/webhook-dropbox.test.ts` (DOC-05):
   - `describe('Dropbox Sign callback handler')` with stubs:
     - `it('returns Hello API Event Received on valid signature_request_all_signed')`
     - `it('returns 401 on invalid event hash')`
     - `it('updates order_documents status to signed')`
   - All `it.todo(...)`

5. `tests/transactions/emails.test.ts` (NOTF-01 through NOTF-04):
   - `describe('email templates')` with stubs:
     - `it('OrderConfirmationEmail renders without errors')`
     - `it('AdminOrderAlertEmail renders without errors')`
     - `it('SigningRequestEmail renders without errors')`
     - `it('DocumentsCompleteEmail renders without errors')`
   - All `it.todo(...)`

6. `tests/transactions/checkout.spec.ts` (PURCH-03, e2e stub):
   - `import { test } from '@playwright/test'`
   - `test.describe('checkout flow', () => { test.fixme('shows order confirmation page after successful checkout') })`

7. `tests/transactions/orders.spec.ts` (PURCH-04, e2e stub):
   - `import { test } from '@playwright/test'`
   - `test.describe('purchase history', () => { test.fixme('renders order cards for buyer') })`
  </action>
  <verify>
    <automated>npm run test:unit -- tests/transactions/ --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 unit test files run (todos counted), 2 e2e spec files exist with fixme stubs. `npm run test:unit -- tests/transactions/` passes.</done>
</task>

</tasks>

<verification>
- `npm run test:unit -- tests/transactions/` runs and reports todo counts
- `npx tsc --noEmit` passes for all new lib files
- `lib/stripe.ts`, `lib/dropbox-sign.ts`, `lib/resend.ts` each export a client instance
- `lib/validations/order.ts` exports schemas and inferred types
</verification>

<success_criteria>
- All Phase 3 npm packages installed
- 3 service client singletons importable from lib/
- Order Zod schemas defined with correct status enums matching DB CHECK constraints
- 7 Wave 0 test files exist with todo/fixme stubs covering all 13 requirement IDs
</success_criteria>

<output>
After completion, create `.planning/phases/03-transactions/03-01-SUMMARY.md`
</output>
