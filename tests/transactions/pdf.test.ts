import { describe, it } from 'vitest'

describe('generatePurchaseAgreement', () => {
  it.todo('returns a non-empty Buffer')
  it.todo('generated PDF contains vehicle VIN')
})

describe('generateBillOfSale', () => {
  it.todo('returns a non-empty Buffer')
  it.todo('generated PDF contains buyer and seller names')
})
