import { describe, it } from 'vitest'

describe('createCheckoutSession', () => {
  it.todo('creates Stripe checkout session for active listing and redirects')
  it.todo('throws error if listing is not active')
  it.todo('throws error if user is not authenticated')
})
