import { describe, it } from 'vitest'

describe('Dropbox Sign callback handler', () => {
  it.todo('returns Hello API Event Received on valid signature_request_all_signed')
  it.todo('returns 401 on invalid event hash')
  it.todo('updates order_documents status to signed')
})
