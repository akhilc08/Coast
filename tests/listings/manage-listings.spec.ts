import { test } from '@playwright/test'

test.describe('Wholesaler: manage listings (LIST-06)', () => {
  test.fixme('dashboard shows Published and Drafts sections', async () => {})
  test.fixme('Edit action navigates to wizard in edit mode with pre-filled values', async () => {})
  test.fixme('Publish action changes listing status from draft to active', async () => {})
  test.fixme('Archive action removes listing from Published section', async () => {})
  test.fixme('wholesaler cannot see or edit listings belonging to another wholesaler', async () => {})
})
