import { test } from '@playwright/test'

test.describe('Vehicle detail page (STOR-05)', () => {
  test.fixme('renders vehicle info: VIN, make, model, year, mileage, price', async () => {})
  test.fixme('renders "Grade Pending" placeholder in grade section', async () => {})
  test.fixme('clicking first photo opens lightbox overlay', async () => {})
  test.fixme('lightbox right arrow advances to second photo', async () => {})
  test.fixme('lightbox ESC key closes the overlay', async () => {})
  test.fixme('documents section is visible to unauthenticated visitors', async () => {})
  test.fixme('Buy CTA button is hidden for unauthenticated users', async () => {})
  test.fixme('Buy CTA button is visible for authenticated consumers', async () => {})
})
