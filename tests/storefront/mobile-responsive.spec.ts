import { test } from '@playwright/test'

test.describe('Storefront mobile responsiveness (STOR-06)', () => {
  test.fixme('at 375px viewport: sidebar filters are hidden', async () => {})
  test.fixme('at 375px viewport: Filters button is visible', async () => {})
  test.fixme('tapping Filters button opens bottom drawer', async () => {})
  test.fixme('at 1280px viewport: sidebar filters are visible, no Filters button', async () => {})
  test.fixme('listing grid shows 1 column at 375px viewport', async () => {})
  test.fixme('listing grid shows 3 columns at 1280px viewport', async () => {})
})
