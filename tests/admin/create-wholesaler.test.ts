import { describe, it } from 'vitest'

describe('createWholesalerAction', () => {
  it.todo('creates user with wholesaler role and returns userId')
  it.todo('rejects duplicate email with error message')
  it.todo('validates input — requires businessName, valid email, password min 8 chars')
  it.todo('rejects non-admin callers')
})
