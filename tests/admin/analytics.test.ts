import { describe, it } from 'vitest'

describe('Admin analytics', () => {
  it.todo('computes total revenue from all completed orders')
  it.todo('counts listings by status')
  it.todo('counts total orders')
  it.todo('counts users by role')
})
