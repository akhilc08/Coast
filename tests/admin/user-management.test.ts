import { describe, it } from 'vitest'

describe('User management actions', () => {
  it.todo('lists all users with role and status')
  it.todo('banUserAction — bans user by setting ban_duration')
  it.todo('unbanUserAction — unbans user by clearing ban_duration')
  it.todo('banUserAction — rejects non-admin caller')
  it.todo('unbanUserAction — rejects non-admin caller')
})
