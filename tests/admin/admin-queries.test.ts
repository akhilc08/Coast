import { describe, it } from 'vitest'

describe('Admin queries', () => {
  it.todo('lists all listings regardless of status')
  it.todo('filters listings by status')
  it.todo('lists all orders')
  it.todo('filters orders by status')
})
